{
  "name": "@dogsvr/cl-tsrpc",
  "version": "0.0.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "protoc": "npx tsrpc proto",
    "build": "rm -rf dist && mkdir dist && cp package.json README.md dist && npx tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dogsvr/cl-tsrpc.git"
  },
  "author": "rowanzhu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dogsvr/cl-tsrpc/issues"
  },
  "homepage": "https://github.com/dogsvr/cl-tsrpc#readme",
  "dependencies": {
    "tsrpc": "^3.4.17"
  },
  "devDependencies": {
    "@dogsvr/dogsvr": "latest",
    "tsrpc-cli": "^2.4.5",
    "typescript": "^5.6.3"
  }
}
